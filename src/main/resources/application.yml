server:
  port: 8282
  servlet:
    context-path: /review-that-matters/

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/review-that-matters
    username: root
    password: Kolkata@1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update